## 🦴 성장판 세포 분류 모델 (Growth Plate Cell Classification)

이 프로젝트는 성장판(Growth Plate) 이미지 데이터에서 추출된 피처를 바탕으로 5가지 주요 세포 타입을 자동 분류하는 머신러닝 파이프라인입니다. 특히, 데이터 불균형 문제를 해결하여 희귀 세포인 Resting 클래스의 검출 성능을 극대화하는 데 초점을 맞췄습니다.

## 🚀 주요 특징 (Key Features)

- 데이터 정제: 노이즈 라벨 제거를 통한 데이터 품질 향상.
- 단계별 실험 설계: 베이스라인부터 튜닝 모델까지 총 5단계의 체계적인 실험 진행.
- 불균형 데이터 최적화: 가중치 학습(Cost-sensitive Learning)을 통한 소수 클래스 성능 개선.
- 하이퍼파라미터 튜닝: RandomizedSearchCV와 PredefinedSplit을 이용한 XGBoost 최적화.

## 📂 프로젝트 구조

```
gp/
├── data/
│   ├── raw/            # 원본 CSV 파일 (1~9번) 
│       ├── test/            # 원본 CSV 파일 (10~11번)
│   └── processed/      # 정제된 통합 데이터
├── src/
│   ├── data_loader.py  # 7:2:2 Group Split 로직
│   ├── model_utils.py  # 모델 생성 및 가중치 계산
│   ├── trainer.py      # 학습 및 평가 리포트 생성
│   └── tuner.py        # 하이퍼파라미터 최적화
├── experiments/        # 각 실험별 결과 (Report, Confusion Matrix 등)
├── main.py             # 전체 파이프라인 실행 스크립트
├── test.py             # test 실행 스크립트
└── config.py           # 주요 설정 값 관리
```

## 📊 실험 결과 

추가 예정

## 🛠️ 설치 및 실행 방법

1. 저장소 클론:

```
git clone https://github.com/jaeseonim/gp.git
```

2. 환경 구축:

```
conda env create -f environment.yml
conda activate gp
```

3. 실행:

```
python main.py
```